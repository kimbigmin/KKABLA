import React from 'react';
import styled from 'styled-components';

function Comment() {
  return (
    <CommentContainer>
      <div>This is going to be comment</div>
    </CommentContainer>
  );
}

const CommentContainer = styled.div``;

export default Comment;
